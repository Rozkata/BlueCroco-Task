import { MultiplierColorPipe } from './multiplier-color.pipe';

describe('MultiplierColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplierColorPipe();
    expect(pipe).toBeTruthy();
  });
});
